apiVersion: "1"
id: "modern.load-boy"
description: "Casc Bundle for Load Controllers"
version: "1"
parent: "modern.parent"
allowCapExceptions: true
jcascMergeStrategy: "errorOnConflict"
jcasc:
  - jcasc
items:
  - items
rbac:
  - rbac
plugins:
  - plugins
#IMPORTANT: This plugin catalog contains parent's description + own description (e.g mock-load-builder)
#Reason: Child plugin catalog overrides parent ones
catalog:
  - catalog
